name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build packages
        run: npm run build

      - name: Prepare docs with built packages
        run: |
          mkdir -p docs/packages/data
          mkdir -p docs/packages/layer-configs/dist
          mkdir -p docs/packages/maplibre/dist
          mkdir -p docs/packages/leaflet/dist
          mkdir -p docs/packages/openlayers/dist
          
          # Copy data package (includes PMTiles file and all JS files)
          cp packages/data/index.js docs/packages/data/
          cp packages/data/version.js docs/packages/data/
          cp packages/data/data_version.js docs/packages/data/
          cp packages/data/india_boundary_corrections.pmtiles docs/packages/data/
          
          # Copy layer-configs dist
          cp -r packages/layer-configs/dist/* docs/packages/layer-configs/dist/
          
          # Copy maplibre dist
          cp -r packages/maplibre/dist/* docs/packages/maplibre/dist/
          
          # Copy leaflet dist
          cp -r packages/leaflet/dist/* docs/packages/leaflet/dist/
          
          # Copy openlayers dist
          cp -r packages/openlayers/dist/* docs/packages/openlayers/dist/

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
